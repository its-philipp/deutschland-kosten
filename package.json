{
  "name": "deutschland-kosten",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check && npm run check:tabellen && npm run check:energie && npm run check:rechnungen",
    "check:tabellen": "node scripts/check-gebuehrentabellen.ts",
    "check:energie": "node scripts/check-energiepreise.ts",
    "check:links": "node scripts/check-links.mjs deutschland-kosten.de",
    "check:rechnungen": "node scripts/check-rechnungen.mjs",
    "check:runtime": "node scripts/check-runtime.mjs",
    "setup:playwright": "npm i --no-save playwright"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.9",
    "@astrojs/preact": "^6.0.1",
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource-variable/bricolage-grotesque": "^5.2.0",
    "@fontsource/ibm-plex-mono": "^5.2.0",
    "@tailwindcss/vite": "^4.0.0",
    "astro": "^7.1.3",
    "preact": "^10.25.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
